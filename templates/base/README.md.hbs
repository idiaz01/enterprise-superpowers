# {{companyName}} Superpowers

Enterprise AI coding superpowers for **{{companyName}}**, generated by [Enterprise Superpowers](https://github.com/idiaz01/enterprise-superpowers).

## Installation

### Claude Code Plugin

```bash
# Install as a Claude Code plugin
claude plugin add ./{{companySlug}}-superpowers
```

Or copy the plugin directory to your Claude Code plugins folder:

```bash
cp -r ./{{companySlug}}-superpowers ~/.claude/plugins/
```

## What's Included

### Design System

Your company's visual identity is baked into the `company-design-system` skill. When Claude creates presentations, HTML prototypes, or documentation, it uses your brand colors, fonts, and logo.

{{#if integrations.length}}
### Integrations

{{#each integrations}}
- **{{this.name}}** - {{this.description}}
{{/each}}
{{/if}}

## Customization

- Edit skills in `skills/` to customize Claude's behavior
- Modify agents in `agents/` to adjust subagent capabilities
- Update hooks in `hooks/hooks.json` to change automation rules
- Adjust design tokens in `config/design-system.json`

## Generated

- **Date**: {{generatedAt}}
- **Version**: {{version}}
- **Generator**: Enterprise Superpowers v{{version}}
